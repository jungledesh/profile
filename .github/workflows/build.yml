name: Build & Security

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  packages: write
  pull-requests: write

jobs:
  scan:
    runs-on: ubuntu-latest
    outputs:
      push: ${{ steps.docker_meta.outputs.push }}
      tags: ${{ steps.docker_meta.outputs.tags }}
      tags_amd: ${{ steps.docker_meta.outputs.tags_amd }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.97.0
          components: clippy, rustfmt

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true

      - name: cargo audit
        uses: actions-rust-lang/audit@v1

      - name: cargo deny
        uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
          arguments: --all-features

      - name: OSV-Scanner (fail on HIGH/CRITICAL)
        run: |
          curl -sSfL https://github.com/google/osv-scanner/releases/latest/download/osv-scanner_linux_amd64 \
            -o /usr/local/bin/osv-scanner
          chmod +x /usr/local/bin/osv-scanner
          sudo apt-get update -qq && sudo apt-get install -y jq
          osv-scanner --lockfile Cargo.lock --format json > osv-results.json
          HIGH_CRIT_COUNT=$(jq '[.results[]?.vulnerabilities[]?.severity[]? | select(.type == "CVSS_V3" and (.score | tonumber >= 7.0))] | length' osv-results.json)
          if [ "$HIGH_CRIT_COUNT" -gt 0 ]; then
            echo "ERROR: Found $HIGH_CRIT_COUNT HIGH or CRITICAL vulnerabilities!"
            jq . osv-results.json
            exit 1
          fi
          echo "No HIGH/CRITICAL vulnerabilities found."

      - name: Semgrep SAST
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
        run: |
          docker pull returntocorp/semgrep:latest
          docker run --rm \
            -v "${{ github.workspace }}:/src" \
            -e SEMGREP_APP_TOKEN \
            -e GITHUB_EVENT_NAME="${{ github.event_name }}" \
            -e GITHUB_SHA="${{ github.sha }}" \
            -e GITHUB_REF="${{ github.ref }}" \
            returntocorp/semgrep:latest \
            semgrep ci

      - name: Check format
        run: cargo fmt -- --check

      - name: Clippy
        run: cargo clippy --locked --all-targets --all-features -- -D warnings

      - name: Tests
        run: cargo test --locked

      - name: Docker tags and whether to push
        id: docker_meta
        run: |
          REG="ghcr.io/jungledesh/profile"
          SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          if [ "${{ github.event_name }}" = "push" ]; then
            echo "push=true" >> "$GITHUB_OUTPUT"
            echo "tags=${REG}:latest,${REG}:sha-${SHA}" >> "$GITHUB_OUTPUT"
            echo "tags_amd=${REG}-amd:latest,${REG}-amd:sha-${SHA}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.event_name }}" = "pull_request" ] && [ "${{ github.event.pull_request.head.repo.full_name }}" = "${{ github.repository }}" ]; then
            echo "push=true" >> "$GITHUB_OUTPUT"
            echo "tags=${REG}:pr-${{ github.event.pull_request.number }},${REG}:sha-${SHA}" >> "$GITHUB_OUTPUT"
            echo "tags_amd=${REG}-amd:pr-${{ github.event.pull_request.number }},${REG}-amd:sha-${SHA}" >> "$GITHUB_OUTPUT"
          else
            echo "push=false" >> "$GITHUB_OUTPUT"
            echo "tags=${REG}:sha-${SHA}" >> "$GITHUB_OUTPUT"
            echo "tags_amd=${REG}-amd:sha-${SHA}" >> "$GITHUB_OUTPUT"
          fi

  build-nvidia:
    needs: scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        if: needs.scan.outputs.push == 'true'
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.repository_owner }} --password-stdin

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push NVIDIA image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: nvidia
          platforms: linux/amd64
          push: ${{ needs.scan.outputs.push == 'true' }}
          tags: ${{ needs.scan.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=min

  build-amd:
    needs: scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo docker image prune --all --force

      - name: Login to GHCR
        if: needs.scan.outputs.push == 'true'
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.repository_owner }} --password-stdin

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push AMD image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: amd
          platforms: linux/amd64
          push: ${{ needs.scan.outputs.push == 'true' }}
          tags: ${{ needs.scan.outputs.tags_amd }}
          cache-from: type=gha
          cache-to: type=gha,mode=min
